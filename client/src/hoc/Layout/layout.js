import React from 'react';
import Header from "../../components/Header/header"

const Layout = () => {
    return (
        <div>
            <Header/>
            
        </div>
    );
};

export default Layout;